# Implementation Plan: SML-1712

## Ticket
**enhance-linear-issues: minor output polish (Step 3 scope block, side-effect note, calibration labels)**

Three low-severity observations from the v3 run review (SML-1693–1698): (1) SML-1698 folded the Step 3 "Confirm Scope" block into prose instead of rendering the template — the skill is silent on whether the block is required verbatim; (2) every save tripped a Linear workspace automation moving the issue Backlog→Todo and into the current cycle, flagged only after the fact — the skill could warn proactively in the scope summary; (3) SML-1693 used mixed labels (a per-field "LightTouch" alongside an overall "Significant"), which reads as contradictory. All three are P4 polish.

## Scope Classification
small — 1 source file (`skills/enhance-linear-issues/SKILL.md`) + 1 repo plan (`docs/plans/SML-1712.md`).

## Overview
Three surgical prose clarifications to `SKILL.md`, all touching the skill's **report/console output to the user** — not the enhancement OUTPUT saved to Linear issues. Step 3 gets a rendering rule (auto may condense, preview renders full) and a Backlog-automation caution. The Enhancement Calibration section gets a "one overall label per issue" rule, cross-referenced from the Output Format. No change to saved issue content, footer/hash, or Step-4 dedup → **skill Version stays at 3**.

## Approach Decision
Small, obvious content edit — short rationale rather than a full comparison table.

### Considered Approaches
**Approach A: Three targeted prose clauses at the named sites (SELECTED)**
- EDIT 1 + EDIT 2 inserted in Step 3 (after "Display scope summary and proceed to analysis.", before "**Batching (>10 issues):**"). EDIT 3 split across the Output Format `recommendation` bullet and a new note under the Enhancement Calibration "Quality Assessment Scale" table.
- Pros: Surgical; each clause sits exactly where the behavior is defined; matches the file's terse imperative voice and `**Bold lead-in:**` rule style; output-preserving (version stays 3).
- Cons: Step 3 grows by two short paragraphs.

**Approach B: Add a new dedicated "Side Effects & Output Notes" section**
- Pros: Centralizes the notes.
- Cons: Over-engineered for three one-line clarifications; breaks the "additions must be proportional / surgical" convention the skill itself preaches; orphans the guidance from the steps it qualifies. Rejected.

### Selection Rationale
Approach A keeps each clarification adjacent to the instruction it qualifies (Step 3 display logic; the calibration scale; the Output Format recommendation), which is how the skill already cross-references. It is the minimal, voice-consistent change and preserves all invariants.

### Adaptation Guidance
Non-negotiable invariants if the exact wording reads awkwardly: (a) Step 3 must state an explicit verbatim-vs-condensed rule (auto = may condense; preview = render full); (b) Step 3 must carry a proactive Backlog→Todo automation caution tied to in-scope Backlog issues, framed as a workspace-side automation the skill does not set; (c) the calibration label must be stated as a single overall per-issue level (not a per-field tag), explicitly naming the contradictory per-field/overall mix as the thing to avoid; (d) **no** change to `**Version: 3**`, footer/hash/dedup, fence count (6), step parity (9/9), or `plugin.json`/`marketplace.json`.

## Files to Modify
- `skills/enhance-linear-issues/SKILL.md` —
  - **Step 3** (after line ~113): insert EDIT 1 (rendering rule) and EDIT 2 (Backlog-automation caution).
  - **Output Format** (line ~294): EDIT 3a — note `recommendation` is exactly one overall level.
  - **Enhancement Calibration** (after the Quality Assessment Scale table, before `### By Issue Type`): EDIT 3b — "one label per issue, not per field" note.

## New Files
- `docs/plans/SML-1712.md` — this plan (repo-committed history; staged in Phase 6).

## Exact Wording

**EDIT 1 — Step 3, rendering rule** (insert after `Display scope summary and proceed to analysis.`):
> **Rendering (verbatim vs. condensed):** The block above is a format guide, not a verbatim script. In **auto** mode — especially a single-issue run — you may condense it to a sentence or two as long as it still conveys the issues in scope and the repo/workspace context; rendering the literal block is not required. In **preview** mode, render it in full so the user can review scope before anything is applied.

**EDIT 2 — Step 3, Backlog-automation caution** (insert immediately after EDIT 1):
> **Backlog-automation caution:** If any issue in scope has a **Backlog** status, add a one-line warning to the scope summary — e.g. `⚠️ Saving a Backlog issue may trip a workspace automation that moves it Backlog→Todo and pulls it into the current cycle.` This is a Linear-side automation, not something the skill sets (it never writes `state`/`cycle` itself); surface it up front so the status change isn't a surprise.

**EDIT 3a — Output Format `recommendation` bullet** (replace the existing bullet):
> - **Quality assessment** and **recommendation** — exactly one overall level (NoChangesNeeded/LightTouch/Moderate/Significant/Decomposed), summarizing the whole issue rather than any single field (see Enhancement Calibration)

**EDIT 3b — Enhancement Calibration, after the Quality Assessment Scale table** (insert before `### By Issue Type`):
> **One label per issue, not per field.** The Enhancement Level is a single *overall* assessment of the issue — the dominant treatment it needs — and it is what the Output Format reports as the **recommendation**. Do not attach a second, field-level calibration label that contradicts it: if the issue overall warrants **Significant** enhancement but its title needs only a light touch, say so in prose ("title: minor fix; description: full restructure") rather than tagging the title "LightTouch" under an overall "Significant". Mixed per-field and overall labels read as contradictory.

## Test Strategy
No automated test framework (content-only plugin repo). Verification = markdown well-formedness + content-coherence review:
- Fence parity stays even at **6**; `**Version: 3**` line unchanged; the 9 Workflow checklist steps still equal the 9 `#### Step N` headers.
- The two Step 3 insertions sit between `Display scope summary...` and `**Batching (>10 issues):**`, using `**Bold:**` lead-ins (not `####`), so step parity is unaffected.
- EDIT 3a/3b are mutually consistent and don't contradict the Output Format, the Calibration scale rows, or the Common Mistakes table.
- Inline code spans only (single backticks); no new ``` fences.

## Implementation Units

1. **polish-step3-and-calibration**: `skills/enhance-linear-issues/SKILL.md`, test: none (content), dependsOn: []
   **Acceptance criteria:**
   - AC1: "Step 3 scope block — explicit verbatim-vs-condensed rule" -> check: Step 3 states auto mode may condense (esp. single-issue) and preview renders in full.
   - AC2: "Backlog→Todo side effect noted proactively in scope summary" -> check: Step 3 carries a conditional caution for in-scope Backlog issues, framed as a workspace automation the skill does not set.
   - AC3: "Calibration labels — per-field vs overall no longer contradictory" -> check: Enhancement Calibration states the level is one overall per-issue label (not per-field) and names the per-field/overall mix as the thing to avoid; Output Format `recommendation` says exactly-one-overall-level.
   - AC4: "Output-preserving" -> check: `**Version: 3**` unchanged; footer/hash/Step-4 dedup unchanged; fence count 6; step parity 9/9; `plugin.json`/`marketplace.json` untouched; only SKILL.md (+ plan) changed; markdown well-formed.

**Unmapped Requirements:** None.

## Shared Interfaces
None — single-unit prose edit.

## Environment & Config Changes
None required.

## Database Changes
None required.

## Impact Map
No code consumers (instructional markdown). Logical consumers of the edited procedure:
- The enhance-linear-issues skill agent executing Step 3 (scope display) and the Output Format / Calibration reporting.
- Large-batch (>10) sub-tasks read this SKILL.md for the static instruction block — verify the new clauses don't contradict the batch path (they touch Step 3 display + calibration reporting, which sub-tasks also follow).

## Invariants to Preserve
- `**Version: 3**` single-source-of-truth line — unchanged (no skill-version bump).
- `.claude-plugin/plugin.json` / `marketplace.json` version `0.3.1` — unchanged.
- Footer format / hash algorithm / Step-4 dedup — unchanged (saved issue content is not touched).
- Fence count 6 (3 pairs); 9 `#### Step` headers == 9 `- [ ] Step` checklist items.
- The Step 3 `Available teams/projects` gate and `REPO_BROWSE_URL` lines — unchanged.
- The Calibration scale rows and Common Mistakes table — unchanged; the new note only clarifies label granularity.

## Verification Targets
- `skills/enhance-linear-issues/SKILL.md` — re-read edited Step 3, Output Format, and Enhancement Calibration after the change; grep that fences=6, Version=3, step parity 9/9.
- `.claude-plugin/plugin.json` / `marketplace.json` — confirm untouched (version not bumped).

## Risk Assessment & Edge Cases

### Risks
- Backlog caution worded as if it always fires (the automation is workspace-specific) → could mislead workspaces without it. Mitigated by "**may** trip a workspace automation … not something the skill sets."
- Calibration note worded so it forbids per-field commentary entirely → too strict. Mitigated by permitting per-field prose, forbidding only contradictory per-field *labels*.

### Edge Cases
| Category | Edge Case | Expected Behavior | Unit |
|----------|-----------|-------------------|------|
| State transition | Auto single-issue run | May condense the Step 3 block to a sentence or two | polish-step3-and-calibration |
| State transition | Preview run | Renders the Step 3 block in full | polish-step3-and-calibration |
| Boundary | No in-scope issue is in Backlog | No Backlog caution emitted (conditional) | polish-step3-and-calibration |
| Integration invariant | Issue overall Significant, one field needs light touch | Describe field treatment in prose; recommendation stays one overall label | polish-step3-and-calibration |
| Integration invariant | Large-batch (>10) sub-task path | Unaffected by structure; sub-tasks follow the same Step 3 display + calibration reporting | polish-step3-and-calibration |

## End-to-End Narrative

After this change, an enhance-linear-issues run behaves as follows:

1. **User invokes the skill on some issues** (UNCHANGED) -> Step 1 announces version (still v3), detects mode.
2. **Step 2 gathers issues + context** (UNCHANGED) -> issues, teams/projects (gated), repo/workspace.
3. **Step 3 confirms scope** (MODIFIED — display only) -> auto mode (esp. single-issue) may condense the scope block; preview renders it in full. If any in-scope issue is in Backlog, a one-line Backlog→Todo automation caution is added. No change to *what* is processed.
4. **Steps 4–5 dedup + process** (UNCHANGED) -> footer/hash/Step-4 dedup identical (version v3).
5. **Output Format report** (MODIFIED — wording only) -> each issue's `recommendation` is exactly one overall level; per-field treatment, when noted, is prose, never a contradictory second label.
6. **Steps 7–9 safety + apply** (UNCHANGED) -> identical saved issue content and footer/hash behavior.

All four acceptance criteria (AC1–AC4) are covered by steps 3 and 5 plus the output-preserving invariants.
