# Implementation Plan: SML-1708

## Ticket
**enhance-linear-issues: add explicit read-before-write guard rail (never draft from titles alone)**

Add an explicit Guard Rail to `skills/enhance-linear-issues/SKILL.md`: never draft or write enhanced content for an issue before fetching and reading its full description and comments. Call it out specifically for the >10 batch / sub-task path (where the original SML-1505–1511 failure occurred). Optionally restate in Step 5 / the Enhancement Process. The corrective rule currently lives only in a session-local memory file from the incident — it is not in the SKILL.md.

## Scope Classification
**small** — 1 source file modified (`skills/enhance-linear-issues/SKILL.md`) + 1 committed plan doc. 1 implementation unit. Non-test (documentation/spec).

## Overview
Three surgical, output-preserving edits to SKILL.md that make the implicit read-before-draft ordering explicit: (1) a new Guard Rail DO NOT item, (2) an explicit read-before-draft clause in the large-batch sub-task path, (3) a one-clause reinforcement at the drafting step. No behavioral change to enhancement output, footer, hashing, or dedup.

## Approach Decision

Small, obvious scope — concise rationale instead of a comparison table.

**Selected:** Append the guard rail as a **new DO NOT item #9** (no renumbering), and reference it by *section name* (not number) from the two callout sites. Rationale: Guard Rail item numbers are cited in historical `docs/plans/*.md` records (SML-1703 "#1", SML-1702 "#7/#8", SML-1707/1701 "DO #6/#5"); inserting at #1 and renumbering would silently invalidate those references and the numbering the ecosystem relies on (#1 Remove anything, #7 team, #8 nesting). Appending is fully surgical. Name-based cross-references ("see Guard Rails") match the house style (Step 5 and DO #6 already reference sections by name, not number) and are robust to future renumbering.

**Adaptation Guidance:** If appending at #9 ever reads as under-emphasized, prominence is supplied by the two cross-reference callouts (Step 5 + step 9) and by referencing the SML-1505 incident in the item text. Non-negotiable constraint: do NOT change enhancement output, the Enhancement Footer format/hash method, Step 4 dedup, or the skill `Version: 3`.

## Files to Modify
- `skills/enhance-linear-issues/SKILL.md` — three additive edits (see Implementation Units).

## New Files
- `docs/plans/SML-1708.md` — this committed plan (version-controlled history).

## Test Strategy
No test framework in repo (content-only plugin). Verification = markdown well-formedness, internal-consistency grep (cross-references resolve by name; "Version: 3" intact; balanced code fences; 9 Workflow-checklist steps == 9 Step headers), scope/diff check, and a 17-point L3 review adapted to instruction-markdown.

## Implementation Units

1. **read-before-write-guardrail**: `skills/enhance-linear-issues/SKILL.md`, test: none (non-test), dependsOn: []
   **Acceptance criteria:**
   - AC1: "Explicit Guard Rail (DO NOT): never draft/write enhanced content before fetching+reading full description AND comments" -> verify: new DO NOT item #9 present, references Enhancement Process steps 1–2.
   - AC2: "Called out specifically for the large-batch sub-task path; each issue's body read before any save_issue" -> verify: Step 5 sub-task path (step 4) contains an explicit read-before-draft clause.
   - AC3: "Optionally restate in Step 5 / Enhancement Process" -> verify: Enhancement Process step 9 reinforced with read-before-draft clause.
   - AC4: "Output-preserving; no forced re-processing" -> verify: `Version: 3` unchanged; no edits to Enhancement Footer, Step 4 dedup, output format, or Auto/Preview/Safety/Decomposition sections.

**Unmapped Requirements:** None.

### Exact edits

**Edit 1 — Guard Rails `### DO NOT:` — append new item #9 (after item 8 "Nest sub-issues"):**

```
9. **Draft from titles alone** — Never draft, enhance, or `save_issue` for an issue before you have fetched and read its full description AND comments (Enhancement Process steps 1–2). Drafting from titles is the skill's most damaging historical failure (the SML-1505 batch: a verified bug was rewritten into an invented feature, then every original had to be restored). In the large-batch sub-task path (Step 5), each issue's body must be read before its proposal is drafted — never enhance from the title or the batch issue-list summary alone.
```

**Edit 2 — Step 5 large-batch path, end of numbered step 4** (currently ends "...the orchestrator applies them in Phase 2)."). Append:

```
 **Read before drafting:** within each sub-task, fetch and read every issue's full description and comments (Enhancement Process steps 1–2) before drafting that issue's proposal — never draft from the title or the batch issue-list alone (see Guard Rails).
```

**Edit 3 — Enhancement Process step 9** (currently "9. Draft enhanced title and description (calibrated to quality gap)"). Replace with:

```
9. Draft enhanced title and description (calibrated to quality gap) — only after steps 1–2 (reading the full description and comments) are complete; never draft from the title alone (see Guard Rails)
```

## Shared Interfaces
None — single file, prose edits.

## Environment & Config Changes
None required.

## Database Changes
None required.

## Impact Map
None — SKILL.md is a runtime-read prompt document with no code importers. The large-batch sub-task path Reads this file at runtime; the edits only strengthen the instructions it reads.

## Invariants to Preserve
- Skill `**Version: 3**` (line 8) — unchanged; this change is output-preserving so it must NOT force re-processing of v3-stamped issues (matches SML-1704..1707 precedent).
- Enhancement Footer save-then-rehash sequence, SHA-256 first-12-hex method, and `date -u` timestamp rule — untouched.
- Step 4 content-hash dedup (both `_..._` and `*...*` footer forms) — untouched.
- Auto/Preview mode flow, Safety Review table, Decomposition criteria, Output Format — untouched.
- Workflow checklist remains 9 steps mapping to 9 Step headers; code fences stay balanced.
- `.claude-plugin/plugin.json` / `marketplace.json` (0.3.1) — NOT touched (SML-1703..1707 commits each changed only SKILL.md; plugin version bumps are batched separately).

## Verification Targets
- `skills/enhance-linear-issues/SKILL.md` — full re-read after edits: cross-references ("Enhancement Process steps 1–2", "see Guard Rails") resolve; no contradiction with implicit fetch-then-draft ordering; `Version: 3` intact.

## Risk Assessment & Edge Cases

### Risks
- Over-prominence vs surgical churn — resolved by append-at-#9 + name-based callouts.
- Accidentally implying a version bump or footer change — mitigated by explicit "do not touch" invariant list and a post-edit grep.

### Edge Cases

| Category | Edge Case | Expected Behavior | Unit |
|----------|-----------|-------------------|------|
| Integration invariant | Re-run of skill on an already-v3-enhanced issue | Still skipped via content-hash dedup (unchanged) — guard rail adds no footer/output change | Unit 1 |
| Boundary | Single-issue inline run (≤10 path) | Guard rail + step 9 clause apply; behavior already compliant (reads at steps 1–2) | Unit 1 |
| Boundary | Large-batch (>10) sub-task path | New explicit read-before-draft clause now governs each sub-task | Unit 1 |
| Consistency | Cross-references | "see Guard Rails" / "Enhancement Process steps 1–2" resolve by name; no broken numeric refs | Unit 1 |

## End-to-End Narrative

After implementation, the skill's read-before-draft contract is explicit at three reinforcing points:

1. **Enhancement Process** (per issue) -> step 1 fetches the issue, step 2 fetches comments (UNCHANGED), then step 9 drafts **(MODIFIED)** — now explicitly gated on steps 1–2 being complete ("never draft from the title alone").
2. **Large-batch (>10) sub-task path** -> each sub-task reads the static block once, and **(NEW)** must fetch+read every issue's full description and comments before drafting that issue's proposal; the orchestrator applies in Phase 2 (UNCHANGED).
3. **Guard Rails DO NOT** -> **(NEW)** item #9 forbids drafting/enhancing/saving before reading the full body+comments, naming the SML-1505 incident and the batch path.
4. Idempotency / output (UNCHANGED) -> `Version: 3`, footer, hash, and Step 4 dedup are untouched, so already-enhanced issues remain skipped and correctly-enhanced output is byte-identical.

All acceptance criteria (AC1–AC4) are covered.

## Version Decision
- **Skill version:** keep **`Version: 3`** — no bump. Guard-rail/process clarification only; does not alter enhancement output, footer format, hash method, or dedup. Bumping would force needless re-processing of every v3-stamped issue. Matches the SML-1704..1707 precedent.
- **Plugin version (plugin.json/marketplace.json):** unchanged at 0.3.1 — out of scope; recent skill-only commits (SML-1703..1707) never bumped it in the same commit.
